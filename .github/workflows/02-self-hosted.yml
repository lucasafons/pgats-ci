name: Self Hosted Test

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test Runner
        run: |
          echo "Running on self-hosted runner"
          hostname
          pwd